#include<stdio.h>
int main()
{
    int i=0,n;

    printf("enter ending ponint:" );
    scanf("%d",&n);
    while(n<=i)
    {
        if(i%2==1)
        {
            printf("%d ",i);
        }i--;   
    }
}